# Workspace

- `devex/` — `@devex/ui`. Follow `devex/AGENTS.md`.
- Cursor consumption: `.cursor/rules/design-system.mdc`. Claude Code: `CLAUDE.md` and `.claude/rules/`.

Do not mention external employers or job applications in repository files.
