import { Meta } from '@storybook/blocks';
import { DualDemo } from './DualDemo';
import { Foundation } from './Foundation';

<Meta title="Guides/Dual reader" />

<Foundation
  title="Dual reader"
  rule="`.` is the product kit. `./docs` is documentation composites, including agent run surfaces. One `DocBlock[]` feeds `renderHuman` and `toAgentMarkdown`."
  why="Meaning that only exists in CSS is a bug. Agent view is markdown of the same blocks, not a second layout. Do not hand-write a parallel string."
>
  <DualDemo />
</Foundation>
