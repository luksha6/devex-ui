import { Meta } from '@storybook/blocks';
import { AppShellDemo } from './AppShellDemo';
import { Foundation } from './Foundation';

<Meta title="Guides/App shell" />

<Foundation
  title="App shell"
  rule="`AppShell` is Nav / named sidebar / article / rail. Empty is named. Tables paginate. Row actions are a Menu. Confirmations are a Dialog. Fail is a reason."
  why="If this page still needs a raw button, the kit is not done. A toast cannot carry the job."
>
  <AppShellDemo />
</Foundation>
