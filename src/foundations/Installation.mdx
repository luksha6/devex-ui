import { Meta } from '@storybook/blocks';
import { specimen } from './Foundation';

<Meta title="Guides/Installation" />

<div className={specimen.page}>
  <header className={specimen.lede}>
    <p className={specimen.heading} role="heading" aria-level={1}>
      Installation
    </p>
    <p className={specimen.rule}>
      Install from npm. Load styles once. Import the barrels. React 18 or 19 is a peer.
    </p>
  </header>
  <div className={specimen.prose}>
    <p>
      The package is <code>@devex/ui</code>. The GitHub repo is <code>luksha6/devex-ui</code>. MIT.
      Catalog:{' '}
      <a href="https://luksha6.github.io/devex-ui/">luksha6.github.io/devex-ui</a>.
    </p>
  </div>
  <pre className={specimen.code}>{`npm i @devex/ui`}</pre>
  <div className={specimen.prose}>
    <p>Then use it. Load styles.css once. The JS entry does not apply CSS.</p>
  </div>
  <pre
    className={specimen.code}
  >{`import '@devex/ui/styles.css';
import { Button } from '@devex/ui';
import { PageMasthead } from '@devex/ui/docs';

export function App() {
  return (
    <>
      <PageMasthead
        crumb="Platform / Knowledge"
        title="Rate limits"
        purpose="What 429 means and how to retry."
        owner="@platform-core"
        verifiedAt="6d ago"
        commit="4a91c02"
        words={1240}
        tokens={2100}
      />
      <Button intent="primary">Continue</Button>
    </>
  );
}`}</pre>
  <table className={specimen.table}>
    <thead>
      <tr>
        <th>Import</th>
        <th>Gets</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td className={specimen.mono}>@devex/ui/styles.css</td>
        <td>Tokens, reset, compiled modules, Onest. Required once per app.</td>
      </tr>
      <tr>
        <td className={specimen.mono}>@devex/ui</td>
        <td>Product primitives: actions, forms, overlays, data, type</td>
      </tr>
      <tr>
        <td className={specimen.mono}>@devex/ui/docs</td>
        <td>
          Documentation composites: PageMasthead, AudienceSwitch, CodeBlock, ParameterTable,
          Callout, ResultRow, FreshnessStamp, SectionAnchor, Stream, ToolCall, RunStatus, plus
          toAgentMarkdown and renderHuman
        </td>
      </tr>
    </tbody>
  </table>
  <p className={specimen.why}>
    Refused: deep-import src/, skip styles.css, or treat this catalog as the package.
  </p>
</div>
