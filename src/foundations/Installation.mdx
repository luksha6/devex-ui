import { Meta } from '@storybook/blocks';
import { specimen } from './Foundation';

<Meta title="Guides/Installation" />

<div className={specimen.page}>
  <header className={specimen.lede}>
    <p className={specimen.heading} role="heading" aria-level={1}>
      Installation
    </p>
    <p className={specimen.rule}>
      Product kit for apps. Pages and run UI in the docs barrel. React 18 or 19 is a peer.
    </p>
  </header>
  <div className={specimen.prose}>
    <p>
      React design system. Product kit for apps. Write a page once: people get layout, agents get
      markdown. UI for watching a run. The package and the GitHub repo are{' '}
      <code>luksha6/devex-ui</code>. MIT.
      Catalog:{' '}
      <a href="https://luksha6.github.io/devex-ui/">luksha6.github.io/devex-ui</a>.
    </p>
  </div>
  <pre className={specimen.code}>{`npm i @luksha6/devex-ui`}</pre>
  <div className={specimen.prose}>
    <p>
      Load styles.css once. The JS entry does not apply CSS. Wrap the app in Theme so tokens and
      the reset apply under a root you own. Read the value with useTheme. Onest ships in that CSS
      file. Overlay primitives and Theme are client components. Paragraphs can hold links.
    </p>
  </div>
  <pre
    className={specimen.code}
  >{`import '@luksha6/devex-ui/styles.css';
import { Button, Page, Theme } from '@luksha6/devex-ui';
import { Run, type DocBlock } from '@luksha6/devex-ui/docs';

export function App() {
  return (
    <Theme value="light">
      <Page>
        <Button intent="primary">Continue</Button>
      </Page>
    </Theme>
  );
}`}</pre>
  <div className={specimen.prose}>
    <p>
      Vite is the snippet above. Next App Router: load styles.css in the root layout. Mark the
      file that mounts Theme, Dialog, Menu, Drawer, Nav, Toast, Tooltip, Popover, or CommandPalette as{' '}
      <code>'use client'</code>. Pass Next <code>Link</code> into TextLink and Nav:{' '}
      <code>{`as={Link}`}</code> / <code>{`linkAs={Link}`}</code>. Button can take{' '}
      <code>href</code> for a painted link.
    </p>
  </div>
  <pre
    className={specimen.code}
  >{`'use client';

import '@luksha6/devex-ui/styles.css';
import { Theme, Page, Button } from '@luksha6/devex-ui';

export default function PageView() {
  return (
    <Theme value="light">
      <Page>
        <Button>Continue</Button>
      </Page>
    </Theme>
  );
}`}</pre>
  <table className={specimen.table}>
    <thead>
      <tr>
        <th>Import</th>
        <th>Gets</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td className={specimen.mono}>@luksha6/devex-ui/styles.css</td>
        <td>Tokens, reset, compiled modules, Onest. Required once per app.</td>
      </tr>
      <tr>
        <td className={specimen.mono}>@luksha6/devex-ui</td>
        <td>Product kit: actions, forms, overlays, layout, data, type, Theme</td>
      </tr>
      <tr>
        <td className={specimen.mono}>@luksha6/devex-ui/docs</td>
        <td>
          Pages, dual reader, and run UI: PageMasthead, AudienceSwitch, CodeBlock, ParameterTable,
          Callout, ResultRow, FreshnessStamp, SectionAnchor, Stream, ToolCall, RunStatus, Run, plus
          toAgentMarkdown and renderHuman
        </td>
      </tr>
    </tbody>
  </table>
  <p className={specimen.why}>
    Refused: deep-import src/, skip styles.css, skip Theme, or treat this catalog as the package.
  </p>
</div>
