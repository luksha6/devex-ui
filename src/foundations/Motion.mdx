import { Meta } from '@storybook/blocks';
import { Foundation } from './Foundation';
import { MotionDemo } from './MotionDemo';

<Meta title="Foundations/Motion" />

<Foundation
  title="Motion"
  rule="Focus-visible is a 2px primary ring that follows the control radius. Fields take it on the border — no offset gutter. 120–180ms opacity on state change. prefers-reduced-motion zeros the duration tokens."
  why="Motion marks a state change. Copy, cite, and approve stay on the control. Toast is for out-of-band work, not Copied."
>
  <MotionDemo />
</Foundation>
