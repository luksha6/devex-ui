---
description: Hex only in Devex tokens. Components use CSS variables.
globs: src/**/*.{css,ts,tsx}
alwaysApply: false
---

# Hex ban

Hex, `rgb()`, and raw color literals belong in `src/tokens/` only. Components use `var(--color-*)`, `--st-*`, `--space-*`. `npm run lint` runs `scripts/ban-hex.mjs`.
